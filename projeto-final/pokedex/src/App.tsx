// src/App.tsx
import { Home } from "./pages/Home";

function App() {
  return (
    // Aqui estamos chamando sua página Home diretamente
    // Mais para frente, vamos trocar isso pelas Rotas (Router)
    <Home />
  );
}

export default App;
